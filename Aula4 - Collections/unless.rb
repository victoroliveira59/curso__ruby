product_status = 'closed'

if not product_status == 'open'  #unless é ao contrário de if/Pode ser usado o if not também para substituir o unless
    check_change = 'can'
else
    check_change = 'can not'
end
  
puts "You #{check_change} change the product"
